name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: | 
          echo "Hello world!"
          echo "Testing multi-line"
          echo "Yey it works"
      - name: Print goodbye
        run: echo "Done - bye!"
